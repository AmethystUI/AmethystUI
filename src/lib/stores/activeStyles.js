import { writable } from "svelte/store";
export let activeStyles = writable({}); // what styles are currently active. We can use this to see which editors we should be enabling for editing
//# sourceMappingURL=activeStyles.js.map